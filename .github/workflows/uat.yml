name: UAT test and deploy
on:
  release:
    types:
      - prereleased

jobs:
  ci:
    name: Feature & Unit Tests
    uses: ./.github/workflows/sonarcloud.yml
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
